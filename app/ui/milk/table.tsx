import Image from 'next/image';
import { UpdateInvoice, DeleteInvoice } from '@/app/ui/milk/buttons';
import InvoiceStatus from '@/app/ui/milk/status';
import { formatDateToLocal, formatCurrency } from '@/app/lib/utils';


export default async function InvoicesTable({
  query,
  currentPage,
}: {
  query: string;
  currentPage: number;
}) {

  return ;
}
